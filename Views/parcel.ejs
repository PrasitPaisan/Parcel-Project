<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/navbar.css">
    <link rel="stylesheet" href="/css/parcel.css">
    <title>Parcel Control</title>
</head>
<body>
    <%- include('navbar')%>
    <div class="container">
        <div class="bodyUse_box">
            <div class="Manage-title-text">
                Manage
            </div>
            <form action="/admin/control/getoutOfUserParcel?id=<%=userData.id%>" method="post">
                <div class="iteminform">
                    <input type="text" name="date" class="input-control" value="<%= userData.date %>" placeholder="date" >
                </div>
                <div class="iteminform">
                    <input type="text" name="name" class="input-control" value="<%= userData.name %>" placeholder="Name">
                </div>
                <div class="iteminform">
                    <select id="company" class="input-control" name="company">
                        <option value="-" <%= userData.company === '-' ? 'selected' : '' %>>Company</option>
                        <option value="Flash" <%= userData.company === 'Flash' ? 'selected' : '' %>>Flash</option>
                        <option value="J&T" <%= userData.company === 'J&T' ? 'selected' : '' %>>J&T</option>
                        <option value="Kerry" <%= userData.company === 'Kerry' ? 'selected' : '' %>>Kerry</option>
                        <option value="DHL" <%= userData.company === 'DHL' ? 'selected' : '' %>>DHL</option>
                    </select>
                </div>
                <div class="iteminform_checkbox">
                    <% if (userData.status === "true") { %>
                        <div class="checkbox_input_zone"><input type="checkbox" name="status" value="false" class="input_control_checkbox"></div>
                        <div class="checkbox_input_alert">
                            <p style="color: rgb(20, 184, 14);">The parcel is still in the box.</p>
                        </div>
                    <% } else { %>
                        <div class="checkbox_input_zone"><input type="checkbox" name="status" value="false" class="input_control_checkbox" disabled></div>
                        <div class="checkbox_input_alert"><p style="color: rgb(244, 34, 34);">The parcel has been received.</p></div>
                    <% } %>
                </div>
                <div class="iteminform-submit">
                    <% if (userData.status === "true") { %>
                        <a type="button" href="/admin?name=<%=userData.date%>" class="button_Back" >Back</a>
                     <button type="submit" class="button_submit" onclick="return confirm('Are you sure?')">Check Out</button>
                    <% } else { %>
                        <a type="button" href="/admin?name=<%=userData.date%>" class="button_Back" >Back</a>
                    <% } %>
                </div>
            </form>
        </div>
        
        </form>
            
        </div>
    </div>

</body>
</html>